<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.andy.bookexchange">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".fbCommentActivity"
            android:label="Facebook"
            android:logo="@drawable/fblogo"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".reading"
            android:label="@string/title_activity_reading"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PageListView"
            android:label="@string/title_activity_page_list_view"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>